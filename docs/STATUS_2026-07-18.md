# プロジェクト状態（2026-07-18 夕方時点）

前任からの引き継ぎセッション（2026-07-17〜18）の到達点。次セッションはここから。

## マイルストーン到達状況

| MS | 状態 | 実測 |
|---|---|---|
| M0 骨組み+評価ハーネス | ✅ 完了 | pytest 112件、3エンジン統合、GT凍結維持 |
| M1 クリーン F1≥0.80 | ✅ **ゲート通過** | holdout（未見8トラック）mean F1 0.887 |
| M2 tab割当+出力 | ✅ 完了 | 運指DP（総当たり検証で最適）、ASCII/MIDI/MusicXML |
| M3 歪みエレキ | 🔶 主要部完了 | エンジン戦略確定（下記）。実アンプ録音での追試が残 |
| M4 リズム量子化 | 未着手 | 設計検討から（v1 失敗箇所） |

## エンジン戦略（実測で確定、DESIGN.md に記録済み）

- **クリーン: MuScriptor**（`--engine muscriptor`、デフォルト=ac+dist プロンプト+cfg1.5、dev F1 0.879）
- **歪み: basic-pitch**（デフォルトエンジン、M1 tuned、合成highgain F1 0.757）
- YourMT3+ はフォールバック統合のみ（歪みで崩壊 0.598、クリーン0.922は学習汚染参考値）

## 評価資産

- `eval_data/guitarset/`（dev 10）/ `guitarset_holdout/`（最終ゲート専用8、チューニング使用禁止）/ `distorted_synth/`（合成歪み2強度×10、GT流用）
- `eval_data/gt/` は人間製・凍結（不可侵）。全実測値は `docs/BENCHMARKS.md`

## 運用メモ

- venv 4本: `.venv`（本体+demucs）/ `.venv-basicpitch`（py3.10）/ `.venv-muscriptor` / `.venv-yourmt3`
- HF_TOKEN は `.env`（gitignore済、起動時自動読込）
- YourMT3+ 本体は `third_party/yourmt3/`（gitignore済 538MB、ライセンス混在のため非同梱）
- MuScriptor cfg≥1.6 は生成暴走 → ランナーに 30 notes/sec 検知あり

## 残課題（優先順）

1. **M4 リズム量子化の設計**（v1 はドラム依存量子化で失敗 → librosa beat tracking 等、音声由来のテンポ・拍推定を独立ステージで）
2. 実アンプ録音での歪みベンチ追試（IDMT-SMT-Audio-Effects 6.45GB、Zenodo直DL可）— 合成ベンチの序列検証
3. クリーン/歪みのエンジン自動使い分け（現状は README の手動指針のみ）
4. MusicXML の五線+TAB 2段構成・タイ対応（M4 の量子化と同時にやるのが効率的）
